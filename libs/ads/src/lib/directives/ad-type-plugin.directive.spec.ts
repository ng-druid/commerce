import { AdTypePluginDirective } from './ad-type-plugin.directive';

describe('AdTypePluginDirective', () => {
  it('should create an instance', () => {
    const directive = new AdTypePluginDirective();
    expect(directive).toBeTruthy();
  });
});
